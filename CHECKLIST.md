# Чек-лист подготовки к деплою на Render.com

## ✅ Перед деплоем

### 1. Подготовка репозитория
- [x] Проект находится в Git репозитории
- [x] Все файлы закоммичены
- [x] `.gitignore` настроен правильно (исключает .env, db.sqlite3, __pycache__)
- [x] Нет чувствительных данных в коде (ключи, пароли)

### 2. Конфигурационные файлы
- [x] `render.yaml` - конфигурация Render
- [x] `Procfile` - команды запуска процессов
- [x] `requirements.txt` - базовые зависимости
- [x] `requirements-prod.txt` - продакшен зависимости
- [x] `.env.example` - пример переменных окружения
- [x] `settings.py` - настроен для продакшена

### 3. Настройки Django
- [x] `DEBUG = False` в продакшене (через переменную окружения)
- [x] `SECRET_KEY` из переменной окружения
- [x] `ALLOWED_HOSTS` настроен через переменную окружения
- [x] Whitenoise настроен для статики
- [x] База данных через `DATABASE_URL` (dj-database-url)
- [x] Статические файлы: `STATIC_ROOT` настроен
- [x] Медиа файлы: `MEDIA_ROOT` настроен

### 4. Зависимости
- [x] `gunicorn` - WSGI сервер
- [x] `whitenoise` - статические файлы
- [x] `psycopg2-binary` - PostgreSQL драйвер
- [x] `dj-database-url` - парсинг DATABASE_URL
- [x] `python-dotenv` - переменные окружения
- [x] `django-cors-headers` - CORS (если нужно)

### 5. Директории
- [x] `static/` - статические файлы существуют
- [x] `media/` - директория для медиа создана
- [x] `templates/` - шаблоны существуют

### 6. Миграции базы данных
- [x] Все миграции созданы
- [x] Миграции применены локально (для проверки)

### 7. Безопасность
- [x] HTTPS включен в настройках
- [x] Secure cookies включены
- [x] HSTS настроен
- [x] XSS защита включена

## 📋 Шаги деплоя

### 1. Подготовка в GitHub
```bash
# Инициализация Git (если ещё нет)
git init
git add .
git commit -m "Ready for Render deployment"

# Создание репозитория на GitHub и пуш
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git branch -M main
git push -u origin main
```

### 2. Создание аккаунта на Render
1. Перейдите на https://render.com
2. Зарегистрируйтесь через GitHub
3. Подтвердите email

### 3. Создание веб-сервиса
1. В панели Render нажмите "New +"
2. Выберите "Web Service"
3. Подключите ваш GitHub репозиторий
4. Render автоматически найдет `render.yaml`
5. Нажмите "Create Web Service"

### 4. После деплоя
1. Дождитесь завершения деплоя (3-5 минут)
2. Получите URL вашего сервиса (например: `https://shopstore.onrender.com`)
3. Обновите `ALLOWED_HOSTS` в `render.yaml`:
   ```yaml
   ALLOWED_HOSTS: shopstore.onrender.com,.onrender.com
   ```
4. Сделайте git commit и push для перезапуска

## 🔍 Проверка после деплоя

### Базовая проверка
- [ ] Главная страница открывается
- [ ] Статические файлы загружаются (CSS, JS)
- [ ] Админка доступна (/admin/)
- [ ] Регистрация работает
- [ ] Логин работает

### Функциональная проверка
- [ ] Каталог товаров отображается
- [ ] Карточки товаров открываются
- [ ] Добавление в корзину работает
- [ ] Оформление заказа работает
- [ ] Профиль пользователя работает

### Техническая проверка
- [ ] Логи без ошибок (Logs в Render)
- [ ] База данных подключена
- [ ] Миграции применены
- [ ] Статические файлы собраны

## 🛠 Решение проблем

### Ошибка: ModuleNotFoundError
```bash
# Проверьте requirements.txt
pip install -r requirements-prod.txt
```

### Ошибка: Static files not found
```bash
# Убедитесь, что whitenoise установлен и настроен
# В settings.py должно быть:
# MIDDLEWARE = [..., 'whitenoise.middleware.WhiteNoiseMiddleware', ...]
```

### Ошибка: Database connection failed
```bash
# Проверьте DATABASE_URL в переменных окружения Render
# В render.yaml должна быть настройка:
# DATABASE_URL:
#   fromDatabase:
#     name: shopstore-db
#     property: connectionString
```

### Ошибка: Bad Request (400)
```bash
# Проверьте ALLOWED_HOSTS
# Добавьте ваш домен в render.yaml
ALLOWED_HOSTS: ваш-домен.onrender.com,.onrender.com
```

## 📊 Мониторинг

- **Логи**: Render Dashboard → Ваш сервис → Logs
- **Метрики**: Render Dashboard → Ваш сервис → Metrics
- **База данных**: Render Dashboard → Databases
- **Переменные окружения**: Render Dashboard → Ваш сервис → Environment

## 🔄 Обновление проекта

```bash
# Внесите изменения
git add .
git commit -m "Описание изменений"
git push

# Render автоматически перезапустит сервис
```

## 💡 Полезные команды

```bash
# Локальная проверка перед деплоем
python manage.py check --deploy

# Проверка миграций
python manage.py showmigrations

# Сборка статики (для проверки)
python manage.py collectstatic --noinput

# Создание суперпользователя на проде (через shell)
python manage.py createsuperuser
```

## 📞 Поддержка

- Документация Render: https://render.com/docs
- Документация Django: https://docs.djangoproject.com
- Troubleshooting Render: https://render.com/docs/troubleshooting
